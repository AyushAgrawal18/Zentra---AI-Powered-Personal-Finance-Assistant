# Zentra Database

This directory contains the database layer for the Zentra platform.

## Structure
- `connection.ts`: Database connection setup.
- `migrations/`: Contains all the SQL migrations (.up.sql and .down.sql files).
- `schema/`: Contains the single monolithic `schema.sql` representation.
- `seeders/`: SQL scripts to seed the database with initial values.